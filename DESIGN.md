# Yeet Design System

Базовая спецификация дизайн-системы приложения **YeetStyle (yeet)** — умного гардероба: пользователь оцифровывает свои вещи, получает образы на каждый день под погоду и повод, ищет похожие вещи по фото и общается с ИИ-стилистом.

> Статус: **v0.3**. Компоненты в Figma (страница 2.0) и в коде (Storybook) синхронизированы; экраны флоу в Figma ещё собраны на исходной коллекции. Разделы с пометкой _TBD_ будут дополняться.
>
> **Storybook** — живая документация для команды и руководства: правила, токены, компоненты, использование во флоу и 15 экранов. Публикуется в GitHub Pages workflow-ом `.github/workflows/storybook.yml`; локально — `npm i && npm run storybook`.

---

## 1. Источник правды

Figma-файл: [YeetStyle 2.0](https://www.figma.com/design/1LAkot5WySMWhwiiFJqJ0e) (`fileKey: 1LAkot5WySMWhwiiFJqJ0e`)

| Страница | ID | Назначение |
|---|---|---|
| **Design System 2.0 (Claude)** | `942:5666` | Исправленная копия дизайн-системы на коллекции «Yeet DS 2.0» (с тёмной темой) — **источник правды для кода** |
| Design System | `551:2286` | Исходная страница дизайн-системы (не редактируется при работе с 2.0) |
| New app design | `70:12` | Полный флоу нового дизайна (~150 экранов и состояний) — контекст |
| Prod (Claude) | `0:1` | Экраны текущего прода (SwiftUI-вьюхи) — для сверки |
| Animations | `354:17404` | Анимации _TBD_ |
| Archive | `352:12171` | Архив, не использовать |

Коллекции переменных (режимы `Light` / `Dark`):
- **«Yeet DS 2.0»** — основная: исправленные имена, заполненная тёмная тема, новые токены `on-accent`, `overlay`, `radius/100`.
- «Yeet Design System» — исходная, используется экранами флоу; тёмная тема в ней не заполнена.

Страница 2.0 разбита на секции: **00 Обзор** (Changelog, Theme preview) · **01 Foundations** · **02 Actions** · **03 Inputs** · **04 Selection** · **05 Navigation & scroll** · **06 Overlays** · **07 Content** · **08 States** · **09 System** · **10 Screen patterns**.
В каждой секции слева — компонент со всеми вариантами, справа — «В флоу»: все случаи его использования с реальными текстами экранов.

---

## 2. Принципы

1. **Одежда — главный герой.** Интерфейс белый и тихий; цвет и фактура приходят из фотографий вещей. Вещи всегда на светло-сером `light-grey` фоне без собственного фона (фон удаляется при съёмке).
2. **Один акцент.** Насыщенный синий `#0100F4` — единственный брендовый цвет интерфейса: главное действие, выбранное состояние, активный фокус.
3. **Мягкая геометрия.** Всё интерактивное — «пилюли» (полное скругление); контейнеры — крупные скругления 20–32.
4. **Характерные заголовки.** Заголовки — засечковый Roboto Slab, весь остальной текст — Inter.
5. **Одна рука.** Основная навигация и действия — внизу экрана (плавающий таб-бар, FAB, bottom sheets).
6. **Разговорный и нейтральный тон.** Обращение на «ты», короткие живые фразы («Полный шкаф, а надеть нечего?»). Приложение для всех — тексты без привязки к полу (см. §8).

---

## 3. Токены

Имена токенов в коде должны повторять имена переменных Figma (с исправлением опечаток, см. §9).

В коде токены разложены на **три слоя** (`src/tokens/tokens.css`). Компоненты читают только семантический и компонентный слои — тема переключается одним атрибутом `data-theme="dark"`.

| Слой | Префикс | Пример | Кто использует |
|---|---|---|---|
| Примитивы | `--yeet-*` | `--yeet-blue-500`, `--yeet-graphite-900` | только слой семантики |
| Семантика | `--color-*`, `--space-*`, `--radius-*`, `--shadow-*` | `--color-bg-canvas`, `--color-text-secondary`, `--color-accent` | атомы, молекулы, организмы |
| Компонентные | `--button-*`, `--card-*`, `--sheet-*`, `--tab-bar-*`, `--input-*` | `--button-primary-bg`, `--sheet-radius` | конкретный компонент |

Соответствие Figma → код: `blue` → `--color-accent`, `black` → `--color-text-primary` / `--color-bg-inverse`, `grey` → `--color-text-secondary`, `light-grey` → `--color-bg-subtle`, `white` → `--color-bg-canvas`, `elevated` → `--color-bg-elevated`, `red` → `--color-danger`, `on-accent` → `--color-text-on-accent`, `overlay` → `--color-bg-overlay`. Полная таблица со свотчами — Storybook → Foundations → Токены.

### 3.1 Цвета интерфейса — `ui-colors/*`

| Токен | Light | Dark | Роль |
|---|---|---|---|
| `ui-colors/blue` | `#0100F4` | `#5B5BFF` | Акцент: primary-кнопки, выбранные чипсы, фокус, FAB, ссылки |
| `ui-colors/blue-10%` | `#0100F4` @ 10% | `#5B5BFF` @ 20% | Фон выбранного чипса / мягкой акцентной кнопки |
| `ui-colors/black` | `#000000` | `#F5F5F7` | Основной текст, secondary-кнопки, snackbar |
| `ui-colors/black-10%` | `#000000` @ 10% | `#F5F5F7` @ 12% | Разделители (`divider`), обводки свотчей |
| `ui-colors/grey` | `#777777` | `#8E8E93` | Вторичный текст, подписи, лейблы полей |
| `ui-colors/light-grey` | `#F7F7F7` | `#26262B` | Фон карточек, полей ввода, segment-control, tertiary-кнопок |
| `ui-colors/light-grey-10%` | `#F7F7F7` @ 10% | `#1F1F23` @ 10% | _TBD_ |
| `ui-colors/white` | `#FFFFFF` | `#0F0F11` | Фон экрана, sheets, диалогов, таб-бара |
| `ui-colors/white-10%` | `#FFFFFF` @ 10% | `#FFFFFF` @ 12% | Кнопки поверх фото/тёмного фона |
| `ui-colors/red` | `#FF4230` | `#FF5A4A` | Деструктивные действия, ошибки, бейдж-уведомление |
| `ui-colors/red-10%` | `#FF4230` @ 10% | `#FF5A4A` @ 18% | Фон деструктивной кнопки («Удалить») |
| `ui-colors/on-accent` | `#FFFFFF` | `#FFFFFF` | Текст и иконки **на** `blue` / `red` — всегда белые |
| `ui-colors/elevated` | `#FFFFFF` | `#1A1A1E` | Поднятые поверхности: tab-bar, sheet, dialog, hint |
| `ui-colors/overlay` | `#000000` @ 40% | `#000000` @ 60% | Затемнение под bottom sheet и диалогом |

**Логика тёмной темы.** Токены названы по цвету светлой темы, а в тёмной работают по роли:
- `white` (фон) и `black` (текст) меняются местами; фон — не чистый чёрный, а графитовый `#0F0F11`, чтобы у фотографий вещей и карточек был воздух.
- `light-grey` → `#26262B` — карточки и поля читаются как слой над фоном; `elevated` (`#1A1A1E`) — таб-бар, sheet и диалоги отделяются от фона без обводок.
- Синий осветлён до `#5B5BFF`: исходный `#0100F4` на тёмном фоне почти не читается (контраст ~2.3:1), новый даёт ~4.4:1 к фону и ~4.8:1 с белым текстом на кнопке.
- Красный чуть осветлён, 10%-подложки усилены до 18–20%, иначе на тёмном они пропадают.
- Кнопка **Black** в тёмной теме становится светлой (как «Sign in with Apple» на тёмном), snackbar — тоже инвертируется.
- Всё, что лежит на синем или красном, берёт `on-accent`, а не `white` — иначе в тёмной теме текст на синей кнопке стал бы тёмным.

### 3.2 Цвета вещей — `item-colors/*`

Палитра для атрибута «цвет вещи» (фильтры, свотчи, color-dot в полях). **Не использовать для UI.** Значения одинаковы в обеих темах — это реальные цвета одежды; на тёмном фоне свотчи всегда рисуются с обводкой `black-10%`, иначе `item-colors/black` сливается с фоном.

| Токен | Значение | Подпись в UI |
|---|---|---|
| `item-colors/black` | `#1A1A2E` | Чёрный |
| `item-colors/grey` | `#777777` | Серый |
| `item-colors/white` | `#FFFFFF` | Белый (со светлой обводкой) |
| `item-colors/purple` | `#6A00FF` | Фиолетовый |
| `item-colors/pink` | `#D900FF` | Розовый |
| `item-colors/green` | `#00D08B` | Зелёный |
| `item-colors/blue` | `#0100F4` | Синий |
| `item-colors/yellow` | `#FFD000` | Жёлтый |
| `item-colors/orange` | `#FF8800` | Оранжевый |
| `item-colors/red` | `#FF4230` | Красный |
| `item-colors/beige` | `#FFE1C7` | Бежевый |
| `item-colors/brown` | `#C26547` | Коричневый |

### 3.3 Типографика

| Стиль | Шрифт | Размер / интерлиньяж | Трекинг | Применение |
|---|---|---|---|---|
| `H1` | Roboto Slab Regular* | 32 / 36 | −1 px | Заголовки экранов («Гардероб», «Вход и регистрация») |
| `H2` | Roboto Slab Regular | 24 / 28 | −0.4 px | Секции, заголовки диалогов |
| `H3` | Roboto Slab Regular | 19 / 24 | −0.3 px | Заголовки sheets, карточек, блоков |
| `Body` | Inter Medium | 14 / 20 | 0 | Основной текст, кнопки, пункты меню, поля |
| `Caption` | Inter Regular | 12 / 16 | 0 | Подписи, мета-данные, бейджи, юридический текст |

\* В макетах H1 использует вариативный вес **380**. Если платформа не поддерживает вариативный Roboto Slab — используем Regular (400).

Правила:
- Одна строка — один стиль, без ручных размеров. Акцентный цвет в заголовке допустим (напр. «Твои образы **на каждый день**» — вторая строка `blue`).
- Вторичный текст — `Body`/`Caption` цветом `grey`.

### 3.4 Отступы — `spaces/*`

Шкала на базе 4 pt: `0, 1, 2, 4, 8, 12, 16, 20, 24, 28, 32, 36, 40, 44, 48, 52, 56, 60, 64, 68, 72`.

Эти же токены в Figma привязаны к высотам компонентов и радиусам, поэтому в коде используем один набор значений для отступов, размеров и скруглений.

### 3.5 Скругления

Радиусы берутся из `spaces/*`, плюс отдельный `radius/100` для круглых элементов. Фактически используются:

| Значение | Где |
|---|---|
| `4` | Handle у sheet |
| `12` | Badge (полное скругление при высоте 24) |
| `16` | Snackbar |
| `20` | Карточки вещей и образов, input-group (XL, L), превью |
| `24` | Input-group M |
| `32` | Все кнопки и пилюли (= «капсула»), верхние углы sheets/dialogs |
| `48` | Плавающий таб-бар |
| `radius/100` | Аватар, круглые элементы |

### 3.6 Тени и прозрачность

| Токен (предлагаемый) | Значение | Где |
|---|---|---|
| `shadow/floating` (эффект-стиль) | Drop shadow 0 / 8, blur 40, `#000000` @ 12% | Плавающий таб-бар, FAB, карточка погоды |
| `opacity/placeholder` | 40% | Плейсхолдер в поле ввода |
| `opacity/caret-idle` | 20% | Каретка в состоянии Focus (до ввода) |

---

## 4. Сетка и раскладка (iPhone)

| Параметр | Значение |
|---|---|
| Базовый экран | 393 × 852 (iPhone 15/16) |
| Боковые поля | 20 |
| Ширина контента | 353 |
| Status bar | 62 |
| Заголовок экрана (H1) | y = 70 от верха экрана |
| Сетка карточек | 2 колонки по 173, промежуток ~8 |
| Таб-бар | плавающий, высота 56, отступ снизу 20 |
| FAB | 56 × 56, справа от таб-бара |
| Минимальная зона нажатия | 40 × 40 (кнопка S); рекомендуемая 44+ |

Правило: экран = `header` сверху + скроллящийся контент + `bottom-nav` или `bottom-bar` снизу. Всё временное (действия, фильтры, выбор) открывается **bottom sheet'ом**, а не новым экраном.

### Скролл и края экрана

- Шапка и нижняя навигация **закреплены**, контент скроллится **под ними**.
- Под `header` — сплошная подложка `white` + полоса затухания 24 (`scroll-edge` Top); над `bottom-nav` — затухание 40, над `bottom-bar` — 24 (`scroll-edge` Bottom). Всё уже встроено в компоненты.
- Затухание сделано альфа-маской поверх `ui-colors/white`, поэтому одинаково работает в светлой и тёмной теме. Не рисовать градиенты вручную фиксированным белым.
- Контент в покое начинается под шапкой (≈ высота `header` − 16) и заканчивается над нижней навигацией (нижний отступ списка ≥ высоты `bottom-nav`).
- Плавающие элементы (tab-bar, FAB, «Показать ещё», hint) — эффект-стиль `shadow/floating`.

---

## 5. Иконки и графика

| Набор | Размер | Состав |
|---|---|---|
| `ui-icons` | 20–24, линейные | Навигация (`chevron-up-down`, `chevron-up`, `chevron-down`, `chevron-left`, `arrow-*`, `external-link`), действия (`plus`, `cross`, `more`, `pen`, `trash`, `archive`, `heart`, `check`, `eye`/`eye-off`, `spin`, `arrows-shuffle`), разделы (`home`, `search`, `search-by-image`, `wardrobe`, `ai`, `camera`, `collage`, `bag-check`), категории одежды (`outerwear`, `top`, `bottom`, `shoe`, `accessories`), служебные (`color`, `placeholder`, `horizontal-drag`, `fingers-pinch`, `container`) |
| `weather-icons` | — | (в 2.0 — ссылки на оригинальные компоненты) 15 состояний погоды × 3 варианта (`clear`, `pcloudy`, `mcloudy`, `fog`, `rain`, `shower`, `tshower`, `tstorm`, `snow`, `lsnow`, `sleet`, `hail`, `windy`, `tornado`, `unknown`) |
| `logos` | — | `yeet` (вордмарк), `apple` |
| `flags-icons` | — | Флаги стран (ISO 3166 коды) для выбора страны (в 2.0 — ссылки на оригинальные компоненты) |
| `shapes` | — | Декоративные формы (напр. «звезда» кнопки «Надеть») |

Иконки наследуют цвет текста компонента (`on-accent` на `blue`/`red`, `white` на `black`, `black` на светлых фонах, `red` на `red-10%`).

Селектор-стрелка ⇕ (выпадающие значения в полях) — `chevron-up-down`; `chevron-up`/`chevron-down` — раскрытие/сворачивание строк.

---

## 6. Компоненты

Система построена по **Atomic Design**: токены → атомы → молекулы → организмы → шаблоны → страницы. Правило зависимостей — только вниз: атом не знает о молекуле, организм не импортирует страницу.

| Уровень | Что это | В коде (`src/`) | В Figma |
|---|---|---|---|
| Tokens | цвет, шрифт, отступ, радиус, тень | `tokens/` | коллекция «Yeet DS 2.0», стили |
| Atoms | неделимые элементы | `atoms/` — Button, IconButton, Icon, Badge, Avatar, ColorDot, Divider, Text, ScrollEdge | 01–02 |
| Molecules | связка атомов с одной задачей | `molecules/` — Field, InputGroup, InputBar, SegmentControl, ChipGroup, ListItem, StatTile, Hint, Snackbar, EmptyState, LoadingState, PhotoTile | 03–04, 08 |
| Organisms | самостоятельный блок экрана | `organisms/` — Header, TabBar, BottomNav, BottomBar, Sheet, Dialog, карточки, PhotoArea, ChatBubble | 05–07 |
| Templates | каркас экрана без данных, скролл и края | `templates/` — Screen | 10 Screen patterns |
| Pages | экран флоу с реальными текстами | `pages/` | New app design |

**Контекст в компонентах.** Компонент описывает, *где* он живёт во флоу: в Figma — фреймы `usage / <component>` справа от компонента, в Storybook — история «В флоу» с названием экрана. Варианты выбираются по роли в контексте (например, `Header` = Large на корневых вкладках, Back на вложенных, Search в поиске), а не по вкусу.


### 6.1 Атомы

Во всех кнопках одна шкала размеров и один набор **семантических** стилей — имена по роли, а не по цвету (в тёмной теме «чёрная» кнопка уже светлая).

| Style | Фон | Текст / иконка | Когда использовать |
|---|---|---|---|
| **Primary** | `blue` | `on-accent` | Главное действие экрана/sheet — одно на экран («Войти», «Применить», «Сохранить и выйти») |
| **Secondary** | `black` | `white` | Сильное альтернативное действие («Войти с Apple») |
| **Tertiary** | `light-grey` | `black` | Второстепенное действие («Сбросить», «Выйти»); невыбранный чипс |
| **Inverse** | `white` | `black` | Кнопка на сером фоне; активный сегмент в segment-control |
| **Ghost** | нет | `black` | Текстовая кнопка, неактивный сегмент |
| **Soft** | `blue-10%` | `blue` | Выбранный чипс / опция |
| **Destructive** | `red-10%` | `red` | Удаление и необратимые действия — **всегда** этот стиль |

| Size | Высота | Паддинг по горизонтали | Gap иконка–текст |
|---|---|---|---|
| XL | 56 | 24 | 12 |
| L | 52 | 20 | 12 |
| M | 48 | 16 | 8 |
| S | 40 | 12 | 8 |

Радиус 32 (капсула), текст — `Body`.

#### `button` — кнопка с текстом

Свойства: `Style`, `Size`, `Show Left Icon` / `Left Icon`, `Show Right Icon` / `Right Icon`, текст.

#### `icon-button` — круглая кнопка с иконкой

Свойства: `Style`, `Size` (56 / 52 / 48 / 40), `Icon`. Примеры: «Назад» (Tertiary), «Отправить» и «+» (Primary), FAB (Primary XL + `shadow/floating`).

#### `badge`

Высота 24, паддинг 8, радиус 12, текст `Caption`. `Style`: Primary, Danger, Secondary, Muted, Tertiary, Ghost.

#### `input-value` — содержимое поля ввода

Свойства: `State`, `Color` (`Black` / `Blue` / `White` / `Red`), `Show Color Dot`, текст. Высота 24, gap 12.

| State | Каретка | Текст |
|---|---|---|
| **Placeholder** | нет | 40% прозрачности |
| **Focus** | есть, 20% | 40% (плейсхолдер) |
| **Typing** | есть, 100% | 100% |
| **Text** | нет | 100% |

`Show Color Dot` — свотч из `item-colors` перед текстом (цвет вещи, чипсы цвета). `Red` — ошибка.

#### Прочие атомы

| Компонент | Спецификация |
|---|---|
| `divider` | Линия 1 px, `black-10%`, ширина контента |
| `snackbar` | 353 × 56, паддинг 20, радиус 16, фон `black`, текст `Body` `white`; `Show Icon` / `Icon` (по умолчанию `cross`) |
| `system / status-bar`, `system / keyboard` | Только для макетов |

### 6.2 Молекулы

#### `input` — строка поля

353 × 56, паддинг 12/20, gap 8. Свойства: `Show Left Icon` / `Left Icon`, `Show Right Icon` / `Right Icon`, `Show Left Value`, `Show Right Value`, `Show Divider`.
По умолчанию — паттерн «ключ — значение»: лейбл `grey` слева, значение + `chevron-up-down` справа. Другие паттерны: поле ввода (только слева), пароль (справа `eye` / `eye-off`).

#### `input-group` — группа полей

Контейнер `light-grey`, радиус **20** во всех размерах, слот `Inputs` (строки `input` с разделителями). `Size`: M 48 / L 52 / XL 56 на строку.

#### `segment-control`

353 × (56/52/48/40), паддинг 4, gap 4, радиус 32, фон `light-grey`. Слот `Buttons`: активный сегмент — `button`/`icon-button` **Inverse**, остальные — **Ghost**. `Content`: Text | Icon. Пример: «Вещи / Образы / Вишлист».

#### `chip-group` — группа чипсов

Слот с gap 4: `icon-button` Primary S («+») + `button` S (**Tertiary** — не выбран, **Soft** — выбран). `Wrap=true` — перенос строк, `Wrap=false` — одна строка с горизонтальным скроллом. Фильтры, поводы, сезоны, стили, пол.

#### `input-bar` — панель ввода

353 × 48: `icon-button` Tertiary M слева, `input-group` M, `icon-button` Primary M справа (`Show Left Button`, `Show Input`, `Show Right Button`). Чат со стилистом, поиск (в т.ч. поиск по странам в sheet).

#### `list-item` — строка списка

Высота 24, ширина FILL, gap 12, текст `Body`. Свойства: `Label`, `Icon`, `Show Trailing` / `Trailing`.

| Type | State | Вид | Где |
|---|---|---|---|
| **Action** | Default | иконка + текст | Action sheet (создать образ, редактировать, удалить…) |
| **Expandable** | Collapsed / Expanded | иконка + текст + `chevron-down` / `chevron-up` | Категории одежды |
| **Radio** | Off / On | круг `light-grey` / `blue` с точкой `on-accent` + текст | Год рождения, страна (с флагом в `Trailing`) |

### 6.3 Организмы

Все организмы — компоненты; экраны и sheet'ы собираются только из них.

#### `sheet` — основа bottom sheet

- Ширина 393, фон `white`, радиус **32 сверху**, паддинг 8 / 20 / 20 / 20, gap 20; подложка под sheet — `ui-colors/overlay`.
- Состав: хэндл (48 × 4, `light-grey`) → заголовок `H3` (`Title`, `Show Title`) → слот **Content** → футер.
- `Footer`: **None** | **Buttons** — пара `button` L 50/50 (`secondary-action` + `primary-action`, стиль и текст правятся прямо из инстанса).
- `Type`: **Modal** — поверх `overlay`; **Panel** — постоянная панель деталей поверх фото (тень `shadow/floating`), раскрывается скроллом (детали вещи/образа, новая вещь, холст образа).
- Фон — `elevated`.

| Тип (контент слота) | Из чего собран | Примеры |
|---|---|---|
| **Action** | `list-item` Action | Действия с вещью, «Добавить в вишлист» |
| **Filter** | `chip-group` / `button` S (Tertiary / Soft) | Сезон, повод, теги, сортировка |
| **Picker** | `list-item` Expandable + чипсы, Footer=Buttons | Категория, цвет (чипсы с `Show Color Dot`) |
| **Range** | гистограмма + двухточечный слайдер (`blue`) | Фильтр цены |
| **Radio** | `list-item` Radio | Год рождения, пол, стиль |
| **Search** | `input-bar` + `list-item` Radio с флагом в `Trailing` | Страна, валюта |
| **Photo** | `photo-tile` × 2 (+ `button` Destructive «Удалить фотографию») | Фото вещи, аватар, поиск по фото |

#### `dialog` — подтверждение

Тот же контейнер, что у `sheet` (фон `elevated`). `Title`, `Description` / `Show Description`, опциональный слот **Content** (`Show Content`) для сложных случаев (удаление аккаунта: плитки статистики, текст с выделением).
`Tone`: **Default** — Tertiary + Primary; **Destructive** — Destructive + Primary. **Безопасное действие всегда синее, опасное — всегда красное.**

#### `header` — шапка экрана

Включает статус-бар, сплошную подложку и затухание снизу. Свойства: `Title`, `Subtitle` / `Show Subtitle`, `Show Action`, `Show Action 2`, `Show Filters`; вложенные `back`, `title-chip`, `action-*`, `search`, `filter-*` правятся из инстанса.

| Type | Состав | Где |
|---|---|---|
| **Large** | H1 + действие справа (+ подзаголовок) | Корневые вкладки: Гардероб, Стилист, Профиль, Поиск |
| **Bar** | «Назад» + title-chip (`button` M Tertiary) + до 2 действий | Новая вещь, Архив, Корзина, детали вещи/образа, создание образа |
| **Back** | «Назад» (+ «Пропустить» Ghost) и H1 ниже | Вход, восстановление пароля, онбординг |
| **Search** | `input-bar` (назад + поле + поиск по фото) + фильтры-дропдауны | Поиск, результаты, поиск по гардеробу |

#### `bottom-nav`

`tab-bar` (+ FAB `icon-button` Primary XL с тенью, `FAB=true`) на подложке с затуханием 40 сверху. Отступ снизу 20.

#### `bottom-bar` — закреплённая кнопка

`button` Primary XL на всю ширину (+ `icon-button` Secondary XL справа, `Secondary Action=true`), подложка с затуханием 24 сверху. «Добавить», «Создать образ», «Переместить в гардероб», «Сохранить образ».

#### `scroll-edge`

Полоса-затухание Top / Bottom (альфа-маска над `ui-colors/white`). Отдельно нужна редко — уже встроена в `header`, `bottom-nav`, `bottom-bar`.

#### `tab-bar`

Плавающая капсула 353 × 56, `white`, радиус 48, паддинг 4, эффект-стиль `shadow/floating`. 5 вкладок (иконки без подписей): **Сегодня** (`home`) · **Поиск** (`search-by-image`) · **Гардероб** (`wardrobe`) · **Стилист** (`ai`) · **Профиль** (аватар с буквой, свойство `Initial`). `Active` — выбранная вкладка, подложка `light-grey`, радиус 32.
FAB «+» — `icon-button` Primary XL рядом с таб-баром (Гардероб, Вишлист). «Назад» — `icon-button` Tertiary в левом верхнем углу.

#### `snackbar`

Над таб-баром: подтверждение действия («Перемещено в архив», «Добавлено в вишлист», «Удалено навсегда»).

#### `hint`

Подсказка поверх холста/фото: пилюля `elevated` + `shadow/floating`, иконка 16 + `Caption` («Перемещай и масштабируй вещи»).

#### Карточки и медиа

| Компонент | Спецификация |
|---|---|
| `item-card` | 173 × 172, радиус 20, `light-grey`, вещь без фона; `Show Discount` / `Discount` (бейдж Danger), `Selected` (галочка) |
| `product-card` | Фото товара + название, цена, магазин (результаты поиска) |
| `brand-card` | Изображение + текст |
| `outfit-collage` | 353 × 353, радиус 20, `light-grey` + точечный паттерн, вещи свободно раскладываются |
| `outfit-thumbnail` | Малое превью образа, радиус 20, `light-grey` |
| `photo-tile` | 173 × 173, иллюстрация + `Label` (галерея / камера) |
| `stylist-prompt-card` | Подсказка в хабе стилиста, радиус 32 |
| `chat-bubble` | Сообщение стилиста |
| `trip-card` | Карточка поездки в карусели |
| `weather-card` | Фон `black`, радиус 20 (один угол 8), иконка погоды + температура + `Caption` |
| `photo-area` | 353 × 353, радиус 20, `light-grey`; Empty — «Добавить фотографию», Photo — фото вещи |
| `stat-tile` | `light-grey`, радиус 20: `Label` (Caption grey) + `Value` (H2); ставится в ряд по 3 с FILL |
| `avatar` | L 96 / M 40 / S 24, круг; Empty — `light-grey` + camera, Initial — `blue` + буква; фото — заливкой |
| Кнопка «Надеть» | Форма-«звезда» `blue` 148 × 148, текст `Body` `on-accent` |

### 6.4 Состояния

| Компонент | Спецификация | Примеры |
|---|---|---|
| `empty-state` | H2 + Body grey по центру, `Action=true` — `button` M Tertiary | «Гардероб пуст», «Образов ещё нет», «Вишлист пуст», «Архив пуст», «Упс, не нашли» + «Сбросить поиск» |
| `loading-state` | иконка `spin` + Body grey | «Удаляем фон», «Собираю образы из твоих вещей…», «Ищем похожие вещи…» |

Пустое состояние ставится по центру свободной области между шапкой и нижней навигацией.

## 7. Флоу приложения (контекст)

Фреймы в Figma называются по схеме **`Раздел / Экран / Состояние`**, напр. `Wardrobe / Items / Empty`, `Auth / Sign In / Password Visible`. Оверлеи: `… / Sheet / …`, `… / Dialog / …`, `… / Toast / …`. Эту же схему используем для имён экранов, превью и сторибуков.

| Раздел | Экраны |
|---|---|
| **App / Onboarding** | Splash (синий фон, вордмарк) → Welcome («Полный шкаф, а надеть нечего?») → Name → First Item (фото + автоудаление фона) → First Outfit |
| **Auth** | Sign In (email/пароль, Sign in with Apple), Password Recovery |
| **Outfits (Сегодня)** | Образ дня под погоду (Sunny / Rain Alert), выбор повода, действие «Надеть», пустое состояние |
| **Search** | По тексту и по фото (кроп), результаты, фильтры цены и сортировки, «Нет результатов» |
| **Wardrobe** | Вещи / Образы / Вишлист, фильтры (категория, сезон, теги, повод), карточка вещи и образа, редактирование |
| **New Item** | Фото → удаление фона → детали (название, категория, цвет, сезон) |
| **Outfit Creation** | Критерии → выбор вещей → холст (жесты, фильтры) → сохранение |
| **Wishlist, Archive, Trash** | Списки, действия, перемещение между ними, очистка |
| **Stylist** | Чат с ИИ-стилистом, каталог, «Образ дня», поездки (Trips) |
| **Profile / Settings** | Аналитика гардероба, редактирование профиля (аватар, пол, стиль, год рождения), страна и валюта, выход, удаление аккаунта, Legal |

Каждый экран проектируется во всех состояниях: **Default / Populated, Empty, No Results, Focused, Filled, Loading**.

---

## 8. Контент и тон

Приложением пользуются и девушки, и парни — **тексты гендерно-нейтральные**.

- Язык интерфейса — русский, обращение на **«ты»**.
- **Никаких родовых окончаний** в глаголах прошедшего времени и прилагательных, обращённых к пользователю. Перестраиваем фразу через настоящее/будущее время, инфинитив, вопрос или существительное:

  | ✗ Не так | ✓ Так |
  |---|---|
  | Забыла пароль? / Забыл(а) пароль? | Не помнишь пароль? |
  | Увидела в соцсетях — найди такую же | Нашёлся образ в соцсетях? Найдём похожее дешевле |
  | Ты готова? | Всё готово? / Начнём? |
  | Рады, что ты с нами, дорогая | Рады, что ты с нами |
  | Ты сохранила образ | Образ сохранён |

- Никаких «(а)», «/а» и прочих скобочных форм — только перестройка фразы.
- Примеры в макетах (имена, почта, одежда) — вперемешку: разные имена, мужские и женские вещи.
- Выбор пола в профиле — `Женский / Мужской / Унисекс`, но **тексты интерфейса от этого выбора не зависят**; пол влияет только на подбор вещей и образов.
- Заголовки короткие и живые, могут быть вопросом («Точно хочешь выйти?», «Очистить корзину?»).
- В диалоге описание объясняет последствия («Все вещи из корзины удаляются навсегда, их уже не вернуть»).
- Текст кнопки — глагол действия («Сохранить и выйти», «Применить», «Надеть»), а не «ОК/Да».

---

## 9. Расхождения в Figma

Статус: ✅ — исправлено на странице **Design System 2.0 (Claude)** и в коллекции «Yeet DS 2.0» (оригинал не тронут); ⏳ — открыто.

1. ✅ Тёмная тема: все `Dark`-значения были `#FFFFFF` → заполнена (§3.1).
2. ✅ Опечатка `ui-colors/balck` → `ui-colors/black`.
3. ✅ Подпись свотча **Red** `#0100F4` → `#FF4230`.
4. ✅ Мусорные переменные `Boolean`, `Sate`, `State`, `Text` — в новую коллекцию не переносились.
5. ✅ Иконки: первый `chevron-up` на деле селектор ⇕ → `chevron-up-down`; описания `chevron-*` и `external-link` исправлены.
6. ✅ Слои `left-inner-sate` / `right-inner-sate` → `…-inner-state`.
7. ✅ Опечатка «Поиска по странам» → «Поиск по странам»; выравнивание шеврона в строке «Обувь» (Picker Sheet «Категория»).
8. ✅ Текст на синем/красном в тёмной теме — новый токен `on-accent`.
14. ✅ Организмы (sheets, диалоги, списки) были нарисованы «сырыми» фреймами — теперь из `button`, `list-item`, `input-bar`; хардкод `#F1F4FF` у выбранных чипсов заменён на Soft (`blue-10%`).
15. ✅ Точка-свотч внутри текста всех кнопок (включённый `Show Color Dot`) — выключена.
16. ✅ Выбранная радиокнопка рисовалась синим кругом со спрятанной иконкой `search` — теперь `list-item` Radio с точкой `on-accent`.
17. ✅ Шевроны категорий брались из стороннего кита (`Icon / Chevron Left`, повёрнутый) — теперь `chevron-down` / `chevron-up` из своих иконок.
9. ⏳ 4 фона коллажей с точечным паттерном (`Section / Content`, `Container / Content 02/03` в «Photo & Image Areas»/карточках) всё ещё привязаны к `light-grey` исходной коллекции — PATTERN-заливку нельзя перепривязать через API, нужно вручную в Figma.
10. ⏳ Два разных «чёрных»: UI `#000000` vs `item-colors/black` `#1A1A2E` — оставлено как есть (цвет вещи ≠ цвет UI).
11. ⏳ Скругление последней карточки в сетке Гардероба — 32 вместо 20 (экран флоу, не трогали).
12. ⏳ Экраны флоу собраны из «сырых» фреймов (`Container / Content 01`) и привязаны к исходной коллекции — перевести на компоненты 2.0 (организмы уже есть).
13. ⏳ Тексты с родом в проде: «Забыла пароль?», «Увидела в соцсетях…» — заменить по §8.

---

## 10. Дорожная карта

- [x] Заполнить Dark-режим
- [x] Унифицировать компоненты: семантические стили, имена свойств, описания; list-item; организмы из компонентов
- [x] Выделить в компоненты: sheet, dialog, tab-bar, карточки, photo-tile
- [x] Шапки, нижняя навигация, закреплённая кнопка, затухание при скролле, пустые состояния, загрузка, аватар, плитки статистики
- [ ] Слайдер цены (range-slider) и холст образа (canvas + жесты) — пока фреймы внутри sheet / экранов
- [ ] Графики аналитики профиля (срезы гардероба) — отдельные компоненты
- [x] Реализация токенов и компонентов в коде + Storybook на GitHub Pages
- [ ] Перевести экраны флоу на компоненты и коллекцию 2.0
- [ ] Состояния нажатия / disabled / loading для кнопок (в макетах пока не встречаются)
- [ ] Токены для теней, прозрачностей, длительностей анимаций (страница Animations)
- [ ] Code Connect: связать компоненты Figma с кодом

### Бэклог продукта (v2, из заметки в Figma)

- Карусель при создании образа: 1 категория — 1 вещь (добавление / удаление категорий)
- Цветовое кодирование поездок
- Календарь вещей и образов
- Маскот
